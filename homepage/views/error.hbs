<!DOCTYPE html>
<html style="overflow: hidden">
    <head>
        <title>Aalstek</title>

        {{> 'head/meta'}}
        {{> 'head/style'}}

        <script src="js/animation.js"></script>
    </head>
    <body class="">
        <div class="fixed w-full">
            {{> 'navigation/reduced/navbar'}}
        </div>

        <main class="flex flex-col h-screen justify-center items-center bg-slate-50">
            <div class="drop-shadow-md flex flex-col p-4 pb-6 justify-center items-center gap-0 border w-5/6 sm:w-3/5 md:w-2/5 border-slate-200 rounded-md bg-white">
                <span class="p-4 text-xl font-semibold transition-all duration-300 text-black hover:text-transparent bg-clip-text bg-gradient-to-r from-red-400 to-rose-600">{{status}} | {{title}}</span>

                <span class="text-xl font-medium p-2 outline-none h-10 rounded-md w-full text-center">{{message}}</span>
                <span class="text-sm text-slate-500 font-medium p-2 outline-none h-10 rounded-md w-full text-center">{{help}}</span>

                {{#if link}}
                <a class="mt-2 text-sm text-slate-500 font-medium p-2 outline-none h-10 rounded-md w-full text-center" href="{{link.url}}">{{link.text}}</a>
                {{/if}}
            </div>
        </main>

        {{> 'navigation/reduced/footer'}}
    </body>
</html>